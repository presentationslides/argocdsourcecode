apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cooler.fullname" . }}-test-connection"
  labels:
    {{- include "cooler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cooler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
